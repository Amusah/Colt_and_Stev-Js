// methods
// functions in objects are called methods

const arith = {
    add: (a,b) => (a + b),
    sub: (a,b) => (a - b),
    div: (a,b) => (a / b),
    mul: (a,b) => (a * b),
    mod: (a,b) => (a % b)
}

console.log(arith.add(2,4));